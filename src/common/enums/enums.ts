export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum Status {
  PENDING = 'pending',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
}
export enum Product_enum{
  ACTIVE = 'active',
  SOLD = 'sold'
}